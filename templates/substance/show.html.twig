{% extends 'base.html.twig' %}

{% block title %}{{'title_substane_show'|trans}}{% endblock %}

{% block body %}
    <h1>{{'title_substane_show'|trans}}</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>{{'label_id'|trans}}</th>
                <td>{{ substance.id }}</td>
            </tr>
            <tr>
                <th>{{'label_name'|trans}}</th>
                <td>{{ substance.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('substance_index') }}">{{'action_back_to_list'|trans}}</a>

    <a href="{{ path('substance_edit', {'id': substance.id}) }}">{{'action_edit'|trans}}</a>

    {{ include('substance/_delete_form.html.twig') }}
{% endblock %}
