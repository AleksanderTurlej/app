{% extends 'base.html.twig' %}

{% block title %}{{'Substance'|trans}}{% endblock %}

{% block body %}
    <h1>{{'Substance'|trans}}</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ substance.id }}</td>
            </tr>
            <tr>
                <th>{{'Name'|trans}}</th>
                <td>{{ substance.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('substance_index') }}">{{'back to list'|trans}}</a>

    <a href="{{ path('substance_edit', {'id': substance.id}) }}">{{'edit'|trans}}</a>

    {{ include('substance/_delete_form.html.twig') }}
{% endblock %}
