{% extends 'base.html.twig' %}

{% block title %}Substance{% endblock %}

{% block body %}
    <h1>Substance</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ substance.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ substance.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('substance_index') }}">back to list</a>

    <a href="{{ path('substance_edit', {'id': substance.id}) }}">edit</a>

    {{ include('substance/_delete_form.html.twig') }}
{% endblock %}
