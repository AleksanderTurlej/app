{% extends 'base.html.twig' %}

{% block title %}{{'Favourites'|trans}}{% endblock %}

{% block body %}
    <h1>{{'Favourites'|trans}}</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ favourite.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('favourites_index') }}">{{'back to list'|trans}}</a>

    <a href="{{ path('favourites_edit', {'id': favourite.id}) }}">edit</a>

    {{ include('favourites/_delete_form.html.twig') }}
{% endblock %}
