{% extends 'base.html.twig' %}

{% block title %}{{'Edit Medicine'|trans}}{% endblock %}

{% block body %}
    <h1>{{'Edit Medicine'|trans}}</h1>

    {{ include('medicine/_form.html.twig', {'button_label': 'Update'|trans}) }}

    <a href="{{ path('medicine_index') }}">{{'back to list'|trans}}</a>

    {{ include('medicine/_delete_form.html.twig') }}
{% endblock %}
