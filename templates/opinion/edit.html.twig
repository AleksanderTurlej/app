{% extends 'base.html.twig' %}

{% block title %}{{'Edit Opinion'|trans}}{% endblock %}

{% block body %}
    <h1>{{'Edit Opinion'|trans}}</h1>

    {{ include('opinion/_form.html.twig', {'button_label': 'Update'|trans}) }}

    <a href="{{ path('opinion_index') }}">{{'back to list'|trans}}</a>

    {{ include('opinion/_delete_form.html.twig') }}
{% endblock %}
