{% extends 'base.html.twig' %}

{% block title %}{{'Opinion'|trans}}{% endblock %}

{% block body %}
    <h1>{{'Opinion'|trans}}</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ opinion.id }}</td>
            </tr>
            <tr>
                <th>{{'Content'|trans}}</th>
                <td>{{ opinion.content }}</td>
            </tr>
            <tr>
                <th>{{'Rating'|trans}}</th>
                <td>{{ opinion.rating }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('opinion_index') }}">{{'back to list'|trans}}</a>

    <a href="{{ path('opinion_edit', {'id': opinion.id}) }}">{{'edit'|trans}}</a>

    {{ include('opinion/_delete_form.html.twig') }}
{% endblock %}
